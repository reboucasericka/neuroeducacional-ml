# NeuroLearn Analytics — dependências
# Pinning leve para reproducibilidade local (sem upgrades agressivos).

# Plataforma web
flask>=3.0,<4
flask-sqlalchemy>=3.1,<4
flask-login>=0.6,<1
flask-wtf>=1.2,<2

# Data Science sintético (pipeline separado do prontuário)
numpy>=1.26,<3
pandas>=2.0,<3
scikit-learn>=1.3,<2
matplotlib>=3.7,<4

# Testes
pytest>=7.4,<9
